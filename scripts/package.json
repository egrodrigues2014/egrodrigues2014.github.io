{
  "name": "portfolio-checks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "QA checkers for the portfolio. Deliberately a separate npm project: the root package.json is regenerated by `hugo mod npm pack` on every install, so a dependency added there would not survive.",
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
